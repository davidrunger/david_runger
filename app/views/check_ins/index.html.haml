- content_for(:page_assets) do
  = ts_tag('check_ins_index')

- if @marriage.other_partner
  = render 'new'
  = render 'check_ins'
- else
  = render 'marriages/proposal_form'

= render 'manage_marriage'
