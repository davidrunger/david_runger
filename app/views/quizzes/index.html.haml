%h1 Quiz results

%p.mb-6 Active quiz functionality has been removed. You can still view the final results of quizzes you already participated in.

%ol
  - @quizzes.each do |quiz|
    %li.mt-2= link_to(quiz.name, quiz)

  - if @quizzes.empty?
    %li You have not participated in any completed quizzes.
